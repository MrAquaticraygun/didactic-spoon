# didactic-spoon
this is a repository for the gps skateboarding integration

## Run locally

From the repository root, run:

```bash
python3 serve.py
```

Then open:

```text
http://127.0.0.1:8000
```

This starts a local host server so the SkateMap PWA can be installed and tested on mobile browsers.
